#include <bits/stdc++.h>
using namespace std;

#define fast_io ios::sync_with_stdio(false); cin.tie(nullptr);
const int MAX_N = 10e5;

int n;
int a[10000];

int main() {
  cin >> n;
  for (int i = 0; i <= n; ++i) {
    cin >> a[i];
  }

  

  return 0;
}